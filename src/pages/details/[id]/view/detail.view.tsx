import React from 'react'
import { useTranslation } from 'react-i18next';
import { DetailProps } from '../types';

export default function Details(props: Readonly<DetailProps>) {
  const { t } = useTranslation();
  return (
    <>
        
    </>
  )
}
